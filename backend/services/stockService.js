import pool from '../db.js';

export const fetchStock = () => {
    // Consultar lista de stock de la base de datos
};

export const saveStockItem = (item) => {
    // Insertar un nuevo ítem de stock en la base de datos
};
