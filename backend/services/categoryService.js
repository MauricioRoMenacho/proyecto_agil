import pool from '../db.js';

export const fetchCategories = () => {
    // Consultar lista de categorías de la base de datos
};

export const saveCategory = (category) => {
    // Insertar una nueva categoría en la base de datos
};
