import * as categoryService from '../services/categoryService.js';

export const getCategories = (req, res) => {
    // Obtener la lista de categorías del servicio y responder al cliente
};

export const addCategory = (req, res) => {
    // Recibir categoría del cuerpo de la petición y guardarla mediante el servicio
};
