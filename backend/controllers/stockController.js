import * as stockService from '../services/stockService.js';

export const getStock = (req, res) => {
    // Obtener la lista de activos del servicio y responder al cliente
};

export const addStockItem = (req, res) => {
    // Recibir activo del cuerpo de la petición y guardarlo mediante el servicio
};
